define(['assembler'], function(assembler){
  module("Assembler Tests");
  test("Test MOV", function() {
    //ok(assembler("MOV R0, #1") == "00C001", "MOV R0, #1");
    //ok(assembler("MOV R1, #1") == "01C001", "MOV R1, #1");
    ok(true, "hello")
  });
});